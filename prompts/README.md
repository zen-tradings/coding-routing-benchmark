# Prompt set `dev_v1`

`dev_v1.jsonl` contains 30 permanent prompts: 10 LOW, 10 MID, and 10 HIGH by the rubric in `PLAN.md`. The expected tier is a benchmark label, not model ground truth. Keep this file unchanged after publishing results; create `dev_v2` for revisions.
